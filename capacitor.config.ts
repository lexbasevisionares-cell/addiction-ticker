import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.addictionticker.app',
  appName: 'Addiction Ticker',
  webDir: 'dist'
};

export default config;
