import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lexbase.addictionticker',
  appName: 'Addiction Ticker',
  webDir: 'dist'
};

export default config;
